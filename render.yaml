services:
  - type: web
    name: athena-hawx-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: TOKEN
        value: SEU_TOKEN_DO_BOT
